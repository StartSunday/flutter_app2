import 'package:flutter/material.dart';

class HomeScreen3 extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Scaffold(
      appBar: AppBar(
        title: Text('111'),
      ),
      body: Center(
        child: Text('123456',style: TextStyle(fontSize: 20, color: Colors.blue),),
      ),
    );
  }

}